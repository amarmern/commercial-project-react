const INITIAL_STATE ={
    currentUser: null
}

const userReducer = (state, action) =>{
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}